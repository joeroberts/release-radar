# Operating baseline preparation

Date: 2026-09-06. Status: authorized preparation; review and owner approval pending.

## Objective and outcome

Update and independently review the agreed operating documents, then establish a
local committed starting baseline containing the full-product plan. Present it for
owner review and approval. Provide the new-task pilot prompt only after approval;
do not launch the product pilot in this task.

## Scope and exclusions

Update root AGENTS.md, ADR-007, the full-product plan and the existing progress
ledger; maintain this brief and its catalog/navigation entries. Preserve the
agreed task/worktree structure, chief architecture responsibility, bounded reviews,
model/effort assignments and absolute ban on Ultra. Preserve all 40 assessed
capabilities and their distinct approval states.

Use the existing local branch `codex/full-product-architecture-plan`, initially
at `1816b99`. Preserve unrelated canonical-checkout changes. This authorizes local
documentation commits and one separate independent review task. It does not
authorize product code, installed hooks/rules/configuration, owner-state mutations,
GitHub publication, push, merge, installation, or execution of C8.

## Dependencies and material risks

The existing plan and ADR-007 provide the baseline. Avoid stale worker contexts,
feature myopia, scope expansion, recursive review, implicit model inheritance and
false claims that a local commit restores application tracking. The canonical
validator rejects ordinary .DS_Store files and the current application registration
has no accepted repository binding; retain these as distinct recovery issues.

## Assignment, verification and acceptance

The current task owns documentation integration and progress. One fresh review task
uses Sol High (`gpt-5.6-sol`, `high`) for consequential operating-policy review,
including architecture/dependency coherence. It reads the candidate in its own
worktree, has no write ownership and does not create additional reviewers or
subagents. Escalation is not needed by default; report a named unresolved issue.
Ultra is prohibited for all work.

Run native documentation validation in the clean planning worktree, inspect the
scoped diff, preserve the managed guidance block, and verify canonical copies.
Read the supported application inventory and report the actual recovery state.
No product test suite is needed for this documentation-only outcome.

Completion requires coherent operating documents, no unresolved required review
findings, a local committed baseline with canonical copies, and explicit reporting
of the separate application recovery limitations. Owner baseline approval remains
the next step. Reviewer recommendations do not expand scope; only required defects
block, and bounded corrections repeat only affected checks.
