# Release Radar Shared Execution v1 adoption

## Approved outcome and scope

The owner approved delivery of the exact prepared adoption patch after Outcome 2.
Add the unchanged block in section 5 of the current
[Shared Execution V1 design](../../../design/shared-execution-integration-v1-design.md#5-exact-consumer-adoption-block)
immediately before the existing `release-radar-guidance:v2:start` marker in root
AGENTS.md, with one blank line separating blocks. Remove or change no existing
clause. Preserve accepted ADRs and independent local fallbacks. This authorizes
that specific governing-file addition, not a general policy rewrite.

## Baseline, assignment and boundaries

Outcome 2 is integrated at 5b17e9a and closed at 9394b9d. Start from the committed
kickoff baseline containing this brief; the coordinator resolves its exact revision
at dispatch. The current uncommitted broad proposal/catalog/index rows are excluded.
The coordinator owns progress.md, catalog closeout and canonical integration. A
fresh delivery task owns only its worktree's AGENTS.md patch and local candidate
commit. A fresh independent reviewer owns no implementation files. No archived
tasks or subagents may be reused. Delivery starts Terra/medium for exact insertion;
review starts Sol/high for governing-clause preservation and failure behavior.
Ceiling Astra/high; never Ultra.

Read catalog/index and applicable current documents only. Installed plugin 0.1.16
and its exact recognized v1 capability were verified during foundation preparation;
repeat observations only where needed for current adoption verification. The app
remains the sole SQLite writer; Codex owns plugin/configuration state. No plugin
installation/update, permissions/hooks, runtime environment management, source
behavior change, other repository adoption, app state/binding/catalog acceptance,
external mutation or deletion is authorized. Outcome 3 remains separate.

## Direct verification and review

Compare the inserted text exactly to the approved current design block and prove
removing only that insertion restores the original AGENTS.md bytes. Compare ADRs
against baseline. Run installed repository-native documentation diagnose/check and
diff hygiene at the exact worktree. Confirm exactly one V1 declaration and retained
v2 tracking block. Independent review must directly inspect the candidate and local
fallbacks for review, authority, external actions, safety and recovery. Observe skill
use in the fresh review task under the adopted instructions and report whether it
was requested explicitly versus selected from local guidance. This does not prove
universal automatic selection, complete isolation or managed app synchronization.
Missing/incompatible/owner-denied environment simulations are not this exact patch.
Only Required findings block; correct them within the same assignment and repeat
only affected checks/review.

## Completion and endpoint

Scoped local candidate commit, fresh independent review, coordinator integration to
local main, installed checks/readback and concise result in the existing ledger.
Close the brief and archive stopped peer tasks after the result is preserved.
No push, PR, remote merge, tag, release, installation or app-state mutation. Retain
existing unrelated files and temporary fixtures; no new report hierarchy or harness.
