# Release Radar 0.1.10 packaging evidence

This record covers the owner-authorized local package and annotated-tag endpoint
for the reviewed responsive-navigation and loading-animation correction. The
release was built from clean version/source commit
`130e90846cd59ed3a181cf3665bbd27cd50d03cd`, based on reviewed navigation
closeout `1d4769ce3289dfd3f5e6fcd3beb6fc35ff713fbd`. The annotated local tag
`v0.1.10` identifies the containing release commit; the build-source revision is
recorded separately to avoid a self-referential package commit.

## Artifact identity

- Repository DMG: `dist/ReleaseRadar-0.1.10.dmg`
- Owner download copy: `/Users/jroberts/Downloads/ReleaseRadar-0.1.10.dmg`
- Size: `8,612,845` bytes
- DMG SHA-256: `a28ee8e47c92fc7256f274448ffc3083a442a09b8eee4a70c89e06ed67d4617b`
- App version/build: `0.1.10 (1)`
- Bundle identifier: `com.rekonlabs.ReleaseRadar`
- Bundled plugin version: `0.1.10`
- Bundled plugin digest: `7f70bcd7a4fac4fe038dc00945b8fb56d3cdd472ec7f7816c40812631107939c`
- Main executable SHA-256: `1493df3303c00e78ae9affb8aef44e9d2d7cf705a09034a2c2530a342396057e`
- CodeResources SHA-256: `e246a98214fc7f60eb04cf93c05249e5f8ba14ed3188648b3e72e30fafdca606`
- App CodeDirectory hash: `d6c7a7dbbfa4fef6934cb8709f2b7e2f38581619`

The DMG contains `ReleaseRadar.app` and an `/Applications` link. `hdiutil`
verified the image checksum and mounted the APFS image read-only. The mounted
bundle's identifier, version, build, executable and `CodeResources` hashes match
the staged bundle exactly. The repository DMG and durable Downloads copy have the
same SHA-256 digest.

## Signing and direct results

The app and every nested executable pass strict signature verification with
Apple Development identity `jaroberts4@gmail.com (PT7GS96H3L)`, team
`2UA854NLX4`, and Hardened Runtime. The main app and Bridge Agent entitlements
match their checked-in entitlement files exactly.

| Check | Runner | Scope | Source | Applicability | Status | Direct result | Limitation |
| --- | --- | --- | --- | --- | --- | --- | --- |
| Version and package contract | `xcodebuild test` | Five focused app/plugin version, digest, recognition and shared-execution checks | Version/source change | workingTree | passed | 5 tests passed with no failures | Existing unrelated compiler warnings remain |
| Signed Release staging | `script/build_and_run.sh stage-release-no-launch` | Release app, framework, helpers, bundle identity and entitlements | `130e90846cd59ed3a181cf3665bbd27cd50d03cd` | exactRevision | passed | Signed app built and staged without launch | Local Apple Development signing only |
| DMG checksum and layout | `hdiutil verify` and read-only mount | `dist/ReleaseRadar-0.1.10.dmg` | Packaged working tree | workingTree | passed | Checksum valid; app and Applications link present; APFS mount reported read-only | Not notarized |
| Mounted nested-code identity | `codesign`, `plutil`, `shasum`, `diff` | Mounted app and all executable nested code | Packaged working tree | workingTree | passed | Strict signatures, team, runtime, versions, plugin, exact entitlements and staged/mounted hashes agree | Gatekeeper distribution acceptance was not claimed |
| Owner process preservation | `pgrep` and `launchctl print` | Installed app, Bridge Agent and lifecycle helper | Local process metadata | workingTree | passed | PIDs remained `74075`, `48541` and `67987` before and after staging, packaging and copy | No app behavior was exercised |

## Installation follow-up

The owner subsequently authorized installing the verified Downloads DMG. Its
SHA-256 was rechecked and `hdiutil verify` passed before a read-only mount showed
that the contained `0.1.10 (1)` application retained the executable and
`CodeResources` hashes above. The mounted application and the staged release used
by the repository's supported install flow therefore had the same signed identity.

Before replacement, the installed `0.1.9 (1)` application was copied to and
verified at:

`/Users/jroberts/Documents/Release Radar Backups/Pre-0.1.10-2026-09-11.ReleaseRadarAppOnly.V5Ojxa/ReleaseRadar.app`

The rollback copy passed strict nested-code verification and matched the prior
installed application's version, build, executable SHA-256
`ac7e312203b3b900fb6e36d4e34be202e495640483f2554e45e7126ce922bf00`,
`CodeResources` SHA-256
`48be20d9762f9b5114017f2eaa4781fa12e627e6a551db27ca9789ab465d9f66`,
and CodeDirectory hash `619175606f973a8482e2ccc2baa4603b04670151`.
Earlier backup directories and DMGs were left unchanged.

`script/build_and_run.sh install-staged-release-no-launch` stopped the exact
installed app and its Bridge and lifecycle services, promoted the verified
candidate to `/Applications/ReleaseRadar.app`, and passed its post-promotion
signature and identity checks. The installed application is `0.1.10 (1)` and
matches the packaged executable, `CodeResources`, and CodeDirectory hashes above.
It launched from `/Applications` as PID `3105`. Normal startup registered Bridge
Agent PID `3111` and lifecycle-helper PID `3110`; executable mappings resolved both
processes to the newly installed application and both embedded executables passed
strict signature verification.

The visible Projects view reopened with the existing Pursuit workspace. An
immediate read-only Settings observation reported the bundled Codex plugin as
**Installed**, with shipped and installed versions both `0.1.10`. This was the
first post-startup observation, and no manual install, reinstall, remove, or
Restart-helper action preceded it; the observation does not independently identify
the internal mechanism that produced the status. The view was returned to Projects
after this check. No plugin-repair action, direct SQLite access, permission change,
database reset, push, tag push, PR, merge, notarization, publication, or cleanup
was performed.

## Owner navigation acceptance follow-up

On September 12, the owner supplied a screenshot showing the installed `0.1.10`
Pursuit Overview loaded with documentation and catalog status current. The owner
confirmed that Overview opened promptly and that the loading animation appeared
while validation ran. The owner then confirmed that Project Plan and Phase Board
opened promptly, and separately confirmed that activating the Pursuit project card
from Projects opened Overview promptly. These observations owner-verify the reported
navigation-responsiveness defect as resolved for those four paths and verify the
loading animation during validation.

The owner did not explicitly confirm rapid switching or latest-destination
retention, fault injection, the complete Phase 6 acceptance guide, or the Phase 8
stale-helper scenario. Those checks remain outstanding and are not implied by this
bounded result. The annotated `v0.1.10` tag remains immutable on release commit
`b18d5296d02a43e5db013a8d5c52e2cf4c1f0c13`; this acceptance record is a later
documentation-only follow-up.
