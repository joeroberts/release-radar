# Release Radar 0.1.17 release candidate — native setup and packaging pending

This is candidate evidence, not a completed release or installation record.
The authorized source baseline is
`89dfc85ad330a30a2ae48d09c7e5252a44ac5111`, including merged
[PR #65](https://github.com/joeroberts/release-radar/pull/65). Candidate metadata
is `0.1.17 (1)`; the installed app remains `0.1.16 (1)` as read from its plist.

## Collision and metadata checks

Setup checks found local and remote release tags through `v0.1.16`. Repository installers
stop at `dist/ReleaseRadar-0.1.16.dmg`; the exact
`/Users/jroberts/Downloads/ReleaseRadar-0.1.17.dmg` path was absent. Downloads
glob enumeration returned no matches despite known earlier installers, so that
listing is inconclusive. Recheck exact destinations before any package write.
Historical tags, installers and recognized version/digest pairs are preserved.

App Debug/Release metadata and the bundled plugin manifest agree on `0.1.17`;
the app build remains `1`. The new four-file normalized plugin digest is
`94d7c1c3c506e3b62f7710e7e84df8ad4fc671b9029d06b1f0369b2882230a07`, using
Core's existing UTF-8 path order, zero separator and big-endian byte length
framing. Only current-release test expectations changed, with a new recognized
pair appended to the registry. Bundled skills, helper/XPC contracts and
entitlements are unchanged.

Direct static assertions passed for both marketing versions, build `1`, manifest
version, normalized digest, the appended recognized pair and preserved 0.1.16
pair. This checks metadata content; it does not execute the new app or helper.

## Native verification and confirmed setup

Main reported static candidate `73ee4aa2fc091461d0f6593f09471c1efc1bab68`
review PASS with no findings from reviewer `01a0a48a`. That metadata review
remains terminal; package verification and documentation-correction review are
separate remaining checks.

The restricted-profile native attempt reached pinned RekonDesignSystem resolution,
then stopped before tests at `sandbox-exec: sandbox_apply: Operation not
permitted`; no sandbox setting was changed and no retry was made. An earlier
owner-terminal result bundle was `notRequested`; a later owner-terminal Debug
build reported success. The designated BuildAgent then directly verified the
agent-run route: its incremental Debug build used existing `DerivedData`, exited
zero with `** BUILD SUCCEEDED **`, and its result bundle reported an arm64 My Mac
build succeeded with zero errors, one warning (ReleaseRadarCore was already
signed and not stripped), and zero analyzer warnings. SwiftPM's default manifest
sandbox remained enabled. This is not a clean rebuild or a restricted-profile
enforcement test; the restricted-profile nested-apply limitation remains.

The BuildAgent build executed **zero XCTest tests**. Separately, the direct
production-reader XCTest path compiled `PluginDigester.swift` with
`InstalledPluginDigesterTests.swift` and executed all **10 containment tests
with 0 failures** in 0.325 seconds. These checks verify the Debug build route and
the reader's synthetic containment cases only; they are not release-version
tests, package verification, or reopened source review.

Framework read access, temporary-directory writes and existing-login Git
configuration were changed by Main/owner, never by this writer. The confirmed
reusable profile, TOML deduplication and shell-startup correction are recorded in
[Native developer setup](../../README.md#native-developer-setup). Main's final
readback confirms the documented toolchain grants and zero global cache-override
exports in `.zshrc`; owner-reported cache-directory creation remains unverified.
The documentation assignment performs no configuration or shell-startup edits.

The authorized source-backed `SWIFTPM_MODULECACHE_OVERRIDE` created
`.build/release-0.1.17/ModuleCache/Swift-1IEYM950OGIQC.swiftmodule` (681 bytes) on
September 15. The module-cache denied-output error then disappeared. After
SwiftPM cache write permission was effective, an own temporary-file probe passed
and was removed, and the `.dia` denied-output error disappeared as well.
This establishes bounded cache routing, not a stable advertised Xcode API or a
successful build. The public `-packageCachePath` attempt had left both denied
manifest paths unchanged.

The last executed native run, before Main's final toolchain-profile readback,
exited `74` with `sandbox-exec: unable to open "system.sb": not found`.
A contemporaneous metadata check of `/System/Library/Sandbox/Profiles/system.sb`
was denied. That historical result does not establish a blocker in the newly
confirmed profile: native readiness has not been rechecked under that final
configuration. SwiftPM sandboxing remained enabled. No identical retry,
alternate executor, sandbox bypass or consolidated rejected profile edit was
performed. The current assignment is documentation-only; no build is run here.

## Remaining release steps

No 0.1.17 bundle/DMG was built, signed, mounted, copied to Downloads, tagged or
installed. Package SHA-256, nested signatures, Hardened Runtime, entitlements,
mounted identity, source-to-payload identity and installed identity remain
unverified for 0.1.17. No notarization or general-distribution trust is claimed.
The designated BuildAgent is the accepted agent-run Debug-build route with
SwiftPM sandboxing retained. Focused release tests, direct signed-package
verification and independent package review remain outstanding. The static
metadata review is complete; Main arranges documentation-correction review
through the existing release reviewer. **Installation is on hold** after the
owner's later instruction and requires an explicit owner resume. No installation
or install-permission change is included in this documentation assignment.

The retained local documentation executable successfully regenerated two indexes
and its `check` command reported that repository documentation indexes match
the catalog. `git diff --check` passed. These are direct repository checks, not
release-version test results or package verification.

Earlier Release Radar/shared-execution/packaging skill reads and CodeGraph
execution were denied; the OpenAI setup skill read was also denied during this
documentation correction. Repository governing instructions and the existing
`script/build_and_run.sh` are the fallback. Repository documentation checking uses
the retained local documentation executable; its result does not establish a new
binary, application binding, catalog acceptance or managed synchronization.

The documentation-only correction passes the retained native documentation
checker, `git diff --check` and `zsh -n` on the setup recipe. The recipe was
syntax-checked without executing a build. All four changed documents retain
their existing catalog identities, paths, lifecycle and not-applicable checksum
policies; no catalog/index regeneration or application acceptance is needed for
this content-only correction. Main's correction review remains pending.

Old `/Users/jroberts/.codex/worktrees/63ab` was absent in the scoped read-only
check. No cleanup occurred. Temporary `.build/release-0.1.17/`,
`.build/native-permissions-build-agent-20260915.log`, its paired result bundle,
and `DerivedData/Build/Products/Debug/ReleaseRadar.app` are retained scratch.
Durable records are this repository brief,
ledger, catalog/indexes and this evidence file. Existing app/installer payloads
remain untouched. No push, PR, merge, database access, app launch, catalog
acceptance or owner-data mutation occurred.
