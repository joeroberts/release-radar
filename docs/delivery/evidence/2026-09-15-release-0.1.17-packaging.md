# Release Radar 0.1.17 package verification

## Candidate and authorization

The owner authorized native packaging, repository/Downloads installers, a normal
Git PR and annotated version-tag publication after independent package review.
**Installation remains on hold.** No app launch, notarization, GitHub Release,
main merge, binding, catalog acceptance or owner-data mutation is included.

The fresh package worktree is on `codex/release-0.1.17-package`, based on
`b8a21539383dedbce9d6b6f9844849f7568203d5`. The clean build-source commit is
`0670e7abe0ea7c876b4c281ed8bf4763487885d2`; it only updates release authority and
assignment documentation. Product source and 0.1.17 (1) metadata are unchanged
from the assigned baseline. The release commit records its generated package;
it is distinct from this build-source commit. No subsequent rebuild is implied.

## Direct results

All checks below ran in the assigned worktree on September 15, 2026. Source
applicability is `exactRevision` at the build-source commit for the native build,
and `workingTree` for generated artifacts and subsequent delivery documentation.

| Check / runner | Status | Direct result and limitation |
| --- | --- | --- |
| `script/build_and_run.sh stage-release-no-launch` | passed | Fresh Release build and staging exited 0, `BUILD SUCCEEDED`. Default SwiftPM sandbox retained; command-local Xcode PATH, Git isolation, temporary directory and SwiftPM cache. No app launch. |
| Script signing/entitlement verification | passed | Strict nested signatures, team `2UA854NLX4`, Apple Development signing, Hardened Runtime, exact approved main/bridge entitlements, and built/staged identity verified. No notarization or general-distribution trust claimed. |
| Direct plist/plugin readback | passed | Built and staged app are `com.rekonlabs.ReleaseRadar`, `0.1.17 (1)`. Source, built and staged plugin versions/digests agree. Registry retains 0.1.17 digest with Shared Execution V1 capability. |
| `hdiutil create -fs APFS -format UDZO`, then `hdiutil verify` | passed | Versioned compressed APFS installer created without overwrite; image checksum valid. |
| Read-only mount and `codesign --verify --deep --strict` | passed | Root contains exactly `ReleaseRadar.app` and `Applications` symlink to `/Applications`. Mounted identity is 0.1.17 (1), signature valid; mount detached. |
| Direct no-follow payload comparison | passed | All 46 built/staged/mounted entries match by file SHA-256, executable permissions and symlink targets. Ordinary recursive diff warned about framework symlink traversal; this direct comparison covered those entries without following links. |
| Exclusive Downloads copy and SHA-256 | passed | Repository and Downloads files are 8,773,068 bytes and have identical SHA-256 below. |
| Built `ReleaseRadarDocumentationTool check --root <assigned-root>` | passed | Repository documentation indexes match catalog. No app binding/acceptance/readback performed. |
| `git diff --check` | passed | Scoped delivery changes have no whitespace errors. |
| XCTest | notRun | **0 tests executed.** Existing version test uses app TEST_HOST; Main explicitly retained no-launch scope. Direct package checks above do not claim XCTest coverage. |
| Independent RO04 package review | notRun | Main dispatches review against the committed package candidate before publication. |

Build warnings include existing Optional `.none` ambiguity, trailing-closure
ambiguity, deprecated string conversion, Swift concurrency/main-actor diagnostics
in ActivityView, and skipped AppIntents metadata extraction. They were not changed
by packaging. Zero compiler errors; no clean full-suite pass is claimed.

## Artifact identity

- Repository: `dist/ReleaseRadar-0.1.17.dmg`
- Downloads: `/Users/jroberts/Downloads/ReleaseRadar-0.1.17.dmg`
- DMG SHA-256: `68bc6da35ff75a23fde777bd8beb67a3802eb2a205da260459a51fbb0ab58c5b`
- App CDHash: `4d6134eeabf53d4314802848be5543c1e5b1cb7b`
- App executable SHA-256: `e99ca5970b986a4a2bb8d78f0d36dac8c3ead20dd3aa629a70643550c028361c`
- Signed CodeResources SHA-256: `37ba18bbab22d2f73c2bb2f5cf92e669295684d9005a879c1be3385046bd0045`
- Plugin normalized digest: `94d7c1c3c506e3b62f7710e7e84df8ad4fc671b9029d06b1f0369b2882230a07`

The existing tracked `dist/ReleaseRadar.app` has nine generated changed files
from this verified staged build. Main expressly included that payload to keep
repository distribution consistent with the new DMG. No product source changed.
Earlier installers and tags remain unchanged. No v0.1.17 tag, branch push or PR
has been performed yet; these follow independent review.

## Retained limitations and disposition

Earlier restricted-profile builds failed at nested sandbox application. The
separate successful incremental Debug build and 10 passing reader containment
tests remain historical evidence from PR #67; they are not this Release build's
test results and do not prove restricted-profile enforcement. Current packaging
uses the supplied full-access runtime without disabling SwiftPM sandboxing.

The installed app was not accessed or changed; the last verified installed
version remains 0.1.16 (1). Installation and runtime acceptance remain deferred.
Catalog artifact IDs, paths, active lifecycle and not-applicable checksums are
unchanged while publication is pending. Application catalog acceptance remains
unverified; repository checks do not establish synchronization.

Durable artifacts are the tracked DMG, existing tracked staged app, this evidence,
release brief/ledger, and the owner-designated Downloads copy. Temporary artifacts
retained in the assigned worktree are `DerivedData/`, `.build/tmp/`,
`.build/swiftpm-module-cache/`, `.build/release-0.1.17-stage.log`,
`.build/dmg-0.1.17/`, and the empty detached `.build/dmg-0.1.17-mount/` directory.
No scratch deletion is requested or performed in this assignment.
