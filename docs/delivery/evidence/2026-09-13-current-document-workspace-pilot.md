# Current-document workspace pilot result

Completed 2026-09-13 (America/New_York). This is evidence of the bounded owner-approved pilot, not authorization for broader isolation, configuration rollout or product implementation. The [brief](../task-briefs/2026-09-13-current-document-workspace-pilot/brief.md) retains the original scope.

## Result

The project-backed desktop route supported a selected current-document workspace, a useful maintenance answer, deliberate historical retrieval and independent review. Both fresh peers activated the unchanged `rr-command-read-probe`; ordinary allowed work completed under its restrictions. The native and desktop fixture tests had already demonstrated allowed reads/writes and denied direct/symlink reads of excluded content. Those terminal fixture tests were not repeated during this pilot.

Canonical source baseline was `d78fef9f308f5e2590b18fd9b6de8360ff76be8f`. The staged set contained the approved pilot brief, a derived source map, unchanged operative AGENTS.md, the managed-documentation design, ADR-001/006/007 and attributed excerpts from the full-product and shared-execution designs. There was no Git object store, archive, completed brief or historical index in the initial tree. Mixed current documents themselves retain historical passages: this was task-relevant selection, not history-free content or demonstrated context-cost reduction. The pre-existing unapproved broad proposal was not supplied.

## Scenario and preserved requirements

The delivery owner answered how a future maintainer should propose a managed-document rename without performing one. Its final independently reviewed answer retained:

- stable artifact identity, canonical path/index updates and distinct legacy versus managed evidence handling;
- repository candidate validation versus separately authorized application catalog acceptance; repository correctness never establishes application synchronization;
- recoverable pre-acceptance restoration, bounded forward completion and post-acceptance forward recovery without invented authority;
- unchanged accepted ADR bytes, qualified M1 approval despite ADR-006's historical pending header, and separate authority for later amendments;
- revision-bound links and immutable proposal facts, without automatic relinking, conflating catalog digests with content revisions or replacing unavailable history with current bytes;
- future portable-package identity/provenance, explicit root mapping/fresh authorization and read-only, freshness-labelled companion copies, without activating those future features.

The last two points were strengthened after the independent reviewer identified one Required omission. The same reviewer accepted the bounded correction; final disposition was Required 0, Optional 0, Out of scope 0.

## Deliberate historical retrieval

After completing its initial current-source answer, delivery requested the historical M1 approval record. The coordinator returned only `docs/delivery/progress.md` at `f6b0fe1538a7ae02be0babe29ecaded22470c10f`, lines 50–57, with current artifact ID `450e84de-703b-4dcd-ad1a-7fddfee0d1d9`. The passage recorded approval but withheld M2, document moves and application actions. Formal catalog metadata at that historical revision was not established and was not inferred from today's metadata. The passage was placed in experimental output, not the default input tree, and used solely as non-authoritative corroboration.

## Direct evidence and limits

| Check | Runner and source | Result | Limitation |
| --- | --- | --- | --- |
| Peer profile and assignment | Coordinator, backend records for both fresh tasks | Passed: exact fixture root, named profile, minimal read, root/excluded deny, work write, restricted network; Sol/high and Astra/high | Initial turns had Full access; owner selected the named profile in each UI before file work |
| Current selection and scenario | Delivery, staged d78fef9 snapshot and approved working-tree brief | Passed after one review correction; actual read set recorded, no default historical tree | No statistical evaluation or proof of reduced context cost |
| Historical question | Coordinator retrieval plus delivery response | Passed: one requested passage, attributed and qualified | Manual mediation; no automatic retrieval broker |
| Source preservation | Coordinator, direct byte comparison with committed source and exact excerpt reconstruction | Passed; canonical ADR diff empty | Staged sources are writable; comparison detects changes rather than preventing them |
| Independent candidate review | Fresh Astra/high task; original assignment, staged sources and frozen output | Passed after focused R1 correction | Separate context and serialized ownership, not enforced role isolation |
| Canonical records | Installed ReleaseRadarDocumentationTool check and git diff check | Passed | Repository-only; no application inventory, binding or catalog acceptance performed |

The delivery task's installed shared-execution skill read was denied outside the workspace; supplied local instructions supported continuation. `shasum` could not start because its Perl dependency was inaccessible; available tooling and coordinator byte comparisons established preservation. These failures were not silently treated as passes or repaired by widening permissions.

Task-reading, connector, web/browser and preloaded-context surfaces remain separately exposed. Shell restrictions do not establish their mediation, network enforcement, immutable staged ADRs, isolation between peer directories or complete historical-access isolation. No source application build, rename, runtime implementation, configuration change or owner-data mutation occurred.

## Ownership and disposition

Delivery: `01a09d80-ca12-7213-a1fb-6388962b3af2`, Sol/high. Independent review: `01a09d80-ccc0-7b71-8b8e-abb2ac05187e`, Astra/high. Both stopped after their assignments; archive state is recorded in [progress](../progress.md). The standing chief architect assessed the pilot scope and receives the result, without another review layer.

Only this concise result, the completed brief and their catalog/index/ledger records are durable pilot deliverables. The staged input, three experimental output files and review output under `/Users/jroberts/CodexPermissionProbe/scratch/work/current-document-pilot`, plus the pre-existing permission fixtures, remain temporary retained test material. No cleanup or deletion is authorized or performed.

The result closes this pilot. Broader document isolation requires separately decided controls for the actual additional access routes; this result does not activate that work, shared-execution adoption, application binding/catalog recovery, or paused product work.
