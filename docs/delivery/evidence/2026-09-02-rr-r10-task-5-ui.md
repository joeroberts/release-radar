# RR-R10 Task 5 verification

This record covers the ticket task count and read-only Tasks inspector in the
existing Phase Board. Independent QA completed the available checks below.
On 2026-09-02, coordinator task `01a06184-6387-7c42-878e-695db0481a18`
explicitly accepted Task 5 under the owner's delegated authority, including
the documented limitations below. This is not a claim of personal human
inspection or successful physical keyboard/spoken VoiceOver verification.

## Direct behavior checks

The focused selection passed 23 tests with no failures. It covers canonical
task ordering, superseded exclusion, checked state, count synchronization,
completion count stability, no-plan omission, isolated query failure, and
recovery without losing the board. The reload tests exercise both idle and
prior phase-selection failure, preserving the latter's error. Existing saved
phase recovery, saving state, startup suppression, and bounded read-only
connection checks also passed.

The new projection tests first failed for the missing projection contract.
Both reload regressions failed against the previous reload behavior before
the bounded corrections. Independent code review covered the projection,
Core read boundary, and reload correction; no required finding remains.

The native hosted rendering test passed at 1600×900 and 760×900. It uses the
actual PhaseBoardView, TicketCardView, TicketDetailView, and AppModel reload
callback with a fresh synthetic store. A sixteen-task fixture includes a long
checked first row; a second ticket has one task. No-plan and failed task reads
exercise the distinct recovery surfaces.

## Native runtime boundary

Interactive verification uses a temporary copy of the Debug XCTest host with
the unique bundle identifier `com.rekonlabs.ReleaseRadar.Task5` and window
title `RR-R10 Task 5 — isolated native acceptance`. The copy retains the
Debug test-host entitlements and has an inert XCTest startup marker even if
LaunchServices opens it after a test exits. Its synthetic stores and captured
images contain no owner delivery data. The production app, bound checkout,
normal SQLite store, and shared bridge are outside this verification.

The reference is [phase_board.png](../../design/mockups/phase_board.png).
The hosted surface starts at the Phase Board; it does not reproduce the
application’s outer sidebar. Wide inspection places Details beside the five
lanes; compact inspection uses the existing stacked board and inspector.

These unmodified native XCTest attachments show the three states:

| State | Wide | Compact |
| --- | --- | --- |
| Loaded | [1600×900](rr-r10-task5-loaded-wide.png) | [760×900](rr-r10-task5-loaded-compact.png) |
| No plan | [1600×900](rr-r10-task5-no-plan-wide.png) | [760×900](rr-r10-task5-no-plan-compact.png) |
| Unavailable | [1600×900](rr-r10-task5-unavailable-wide.png) | [760×900](rr-r10-task5-unavailable-compact.png) |

## Native macOS text sizing

Apple documents that `dynamicTypeSize` does not affect text size on macOS:
[EnvironmentValues.dynamicTypeSize](https://developer.apple.com/documentation/swiftui/environmentvalues/dynamictypesize).
Apple's [typography guidance](https://developer.apple.com/design/human-interface-guidelines/typography)
also identifies Dynamic Type as unsupported on macOS.

At both widths the test delivered `.large` and `.accessibility3` to the native
NSHostingView. The environment changed, but the scaled metric remained 12
points and the rendered text did not enlarge. These checks establish the
platform limitation; they are not evidence of enlarged-text behavior. No
custom scaling feature or global font setting was added. Delegated UI
acceptance explicitly includes this platform limitation.

## Interactive acceptance

Independent QA confirmed sixteen separate read-only accessibility elements
with complete labels, titles, checked/unchecked state, and item positions at
wide and compact widths. Cards expose native button roles, singular/plural
counts, and no separate task action or completed/total fraction. No-plan
selection, inspector scrolling, and actual Increase Contrast were exercised
at both widths.
The independent external accessibility trees are preserved for
[wide](rr-r10-task5-accessibility-wide.txt) and
[compact](rr-r10-task5-accessibility-compact.txt) windows. The
[wide increased-contrast capture](rr-r10-task5-increased-contrast-wide.png)
shows the actual authorized system setting applied to the test window.
The [compact contrast capture](rr-r10-task5-increased-contrast-compact.png)
preserves the long first title and metadata; the
[scrolled compact capture](rr-r10-task5-compact-last-tasks.png) reaches Task 16.
The native inspector's exposed Scroll Down/Up actions reached the last tasks
and returned to the first rows without a hidden disclosure.

The long interactive run exited with code 0 before completing its remaining
checks; XCTest marked it failed. The UI tool then opened the guarded inert
startup window. QA rejected that window's compact contrast capture as invalid
evidence and stopped interacting with it. This does not invalidate the
separate successful, uninterrupted native rendering test above.

Physical clicks intermittently reported no available window while accessibility
inspection and actions worked. Tab did not move focus to cards, even after
temporarily enabling Keyboard Navigation. Keyboard activation and spoken
VoiceOver navigation remain unverified. The coordinator explicitly accepted
this residual verification risk; neither check is recorded as passing, and
no passing result is inferred from source or static images.

A shorter follow-up used the unchanged fixture with 60-second inspection
stages and passed at 07:26:54 EDT on 2026-09-02 (one test, zero failures).
The actual Reload button was activated through accessibility at both widths:
the failed ticket regained its count and all sixteen rows, the unavailable
surface disappeared, and the unrelated one-task ticket and five lanes stayed
usable. The hosted test independently asserted the recovered canonical count
after each external button activation. No required product defect remains
from the independent projection/read-boundary and QA/UX reviews.

Under explicit coordinator authorization, QA temporarily changed Increase
Contrast, VoiceOver, and Keyboard Navigation. Each immediately observed prior
value was off. Increase Contrast automatically enabled Reduce Transparency;
turning it off restored that setting too. Final readback confirmed Increase
Contrast, Reduce Transparency, Keyboard Navigation, and VoiceOver off. No
accessibility/security permission or Full Keyboard Access setting was changed.

## Delivery boundary

The coordinator reviewed the canonical record, representative loaded,
no-plan, unavailable and contrast captures, and the compact external
accessibility tree against the approved design. Acceptance released the
already authorized commit, PR, and merge workflow for Task 5 only. Task 6
remains unopened until the coordinator verifies integration.

This is repository-prepared work. The catalog remains pending separately
authorized deployment and application acceptance. Temporary builds, test
results, copied hosts, screenshots, and synthetic fixture directories are
retained; they are not controlling artifacts and no cleanup is authorized.
